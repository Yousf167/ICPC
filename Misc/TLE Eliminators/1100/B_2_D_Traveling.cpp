#include <bits/stdc++.h>
using namespace std;
int main()
{
      int t;
      cin>>t;
      while(t--)
      {
            int n, k, a, b;
            cin>>n>>k>>a>>b;
            vector<pair<long long, long long>> coords(n);
            for(int i = 0; i < n; i++)
            {
                  long long xi, yi;
                  cin>>xi>>yi;
                  coords[i] = {xi, yi};
            }

            
      }
}